services:
  - type: web
    name: jobwatch-frontend
    runtime: docker
    region: oregon
    plan: free
    healthCheckPath: /
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
